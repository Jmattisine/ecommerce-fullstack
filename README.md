# Ecommerce Fullstack (React + Node + Mongo)

Monorepo con:
- client/ (React + Vite)
- server/ (Express + Mongo + JWT)

## Local
- Backend: `cd server && cp .env.sample .env && npm i && npm run dev`
- Frontend: `cd client && cp .env.sample .env && npm i && npm run dev`
